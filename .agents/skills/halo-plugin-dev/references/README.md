# references mapping

来源：`powers/halo-plugin-dev/steering/*.md`

## 分组
- `ui/`：UI 扩展与前端集成
- `api/`：API 与客户端对接
- `runtime/`：运行时模型、后端扩展点与索引注册/查询
- `governance/`：权限、配置、通知、诊断

## 同步规则
1. steering 变更后先同步本目录。
2. `SKILL.md` 仅维护路由规则、输入输出、约束与流程。
3. 仅当域边界变化时才修改 `SKILL.md`。
